
# Car Rental Booking Website

A car rental booking web application built with React, Vite, and TailwindCSS with a Supabase backend.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

Run `npm run dev -- --host` to start the development server on network.
